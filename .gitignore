# OPTIONAL: Ignore data if it is downloaded/generated by the script
# Keep raw inputs out of git
data/raw/
*.gz
*.json.gz

# Ignore processed outputs by default...
data/processed/*
# ...but KEEP the files required to run the Three.js map
!data/processed/
!data/processed/omphalos_map.json
!data/processed/sky_ed.json
!data/processed/gravity_sources.json
!data/processed/guardian_map.json

# Other generated data folders (keep out of git)
data/guardian_sources/
data/lore_samples/

# Large dumps / archives
_all_files_dump*.txt
_llm_chunks*
*.zip

# Python caches (anywhere)
**/__pycache__/
**/*.pyc

